import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vika.ionic-test',
  appName: 'ionic-poc2',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
